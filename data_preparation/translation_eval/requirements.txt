# Translation eval pipeline — pip dependencies on top of the existing
# data_preparation conda environment. Install with:
#   pip install -r requirements.txt
#
# unbabel-comet pulls in torch + transformers. On macOS this is a CPU build
# by default (~1.5 GB install). If you have a CUDA box, install torch first
# with the matching CUDA wheels.

openai>=1.50.0
pydantic>=2.0
pandas>=2.0
tqdm>=4.66
python-dotenv>=1.0

# FLORES-200 download
datasets>=2.18.0
huggingface-hub>=0.20.0

# Metrics
unbabel-comet>=2.2.2
sacrebleu>=2.4.0
