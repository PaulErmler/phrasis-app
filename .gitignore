# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# plans 
/plans

# convex temp directory
.convex-tmp

# local convex branch snapshots
.convex-snapshots/

# typescript
*.tsbuildinfo
next-env.d.ts

# build identity, written by scripts/build-id.mjs on every `pnpm build`
.build-id.json

data_preparation/translation_eval/data

# data preparation 
data_preparation/data/intermediate_outputs
data_preparation/data/inputs
data_preparation/data/ARCHIVE
data_preparation/data/output/dataset_statistics
data_preparation/data/output/sentences_by_difficulty
data_preparation/sentence_processor
data_preparation/language_intelligence/output
data_preparation/language_intelligence/utils/output
data_preparation/pipeline.py
**/__pycache__/**
tts-output
voice-examples


data_preparation/language_intelligence/spacy_classifier_documentation.md
.gitignore
data_preparation/data/output/sentences_with_topics_cache.tsv
playwright-report/index.html
test-results/.last-run.json
.gitignore
# Test artifacts
coverage/
playwright-report/
test-results/
# Generated by e2e/auth.setup.ts — one storage-state + one credentials file
# per fixture user (currently user-a / user-b). Whole directory ignored so
# adding a third fixture doesn't need a .gitignore touch.
e2e/.auth/

# ElevenLabs voice review output (generated by scripts/check-elevenlabs-voices.ts)
scripts/voice-samples/

# TTS prototype playground cache (generated by scripts/tts-prototype.ts)
scripts/tts-samples/

# OGTE dataset (data files only — scripts are committed)
data_preparation/ogte-dataset/data/

# One-shot Tagalog sample generator + its output
scripts/_tmp-tagalog-sample.ts
tagalog-sample/
.claude/scheduled_tasks.lock

# Local Claude Code worktrees — never commit
.claude/worktrees/
native/shell/app-url.js

# Kanban Markdown extension — local task board (see LachyFS.kanban-markdown)
/.devtool

# Kanban board — symlink into Paul's Obsidian vault (machine-specific path,
# see CLAUDE.md). Recreate with:
# ln -s "$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/PaulsVault/projects/Flexling" board
/board
store-assets/
