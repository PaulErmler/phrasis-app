<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Daily reminder pushes. A runtime permission since API 33, and
         targetSdk is 36 — without it Android silently drops every
         notification. Requested from JS by @capacitor/push-notifications'
         requestPermissions() (see hooks/use-push-registration.ts).
         NOTE: no default_notification_icon meta-data is set, so Android falls
         back to the launcher icon. A proper monochrome small icon is a
         follow-up — see docs/store/push-notifications-setup.md. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Voice recording (chat voice input & pronunciation practice). The
         Capacitor WebChromeClient bridges getUserMedia permission requests
         to this Android permission automatically. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
</manifest>
